name = "blvm-governance"
version = "0.1.4"
description = "Governance webhook and economic node tracking module for blvm-node"
author = "Bitcoin Commons Team"
entry_point = "blvm-governance"

capabilities = [
    "read_blockchain",
    "subscribe_events",
]

[config_schema]
webhook_url = "Optional governance webhook URL for proposal notifications"

# Written by release CI — do not edit by hand.
